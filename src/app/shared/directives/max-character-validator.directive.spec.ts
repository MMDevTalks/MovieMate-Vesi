import { MaxCharacterValidatorDirective } from './max-character-validator.directive';

describe('MaxCharacterValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new MaxCharacterValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
